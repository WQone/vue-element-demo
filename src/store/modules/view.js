const state = {};

// getters
const getters = {};

// mutations
const mutations = {
  ClearForm() {},
};

const actions = {
  ClearForm({ commit }) {
    commit('ClearForm');
  },
};

export default {
  state,
  getters,
  actions,
  mutations,
};
