const state = {};

// getters
const getters = {};

// mutations
const mutations = {
  ClearForm(state) {},
};

const actions = {
  ClearForm({ commit }) {
    commit('ClearForm');
  },
};

export default {
  state,
  getters,
  actions,
  mutations,
};
